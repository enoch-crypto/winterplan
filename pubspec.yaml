name: enochwinplan
description: "A new Flutter project."
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI 与 状态管理
  provider: ^6.1.2
  fl_chart: ^0.68.0
  google_fonts: ^6.2.1
  intl: ^0.19.0
  
  # 数据存储
  shared_preferences: ^2.3.2
  
  # 文件与路径 (新增备份功能需要)
  path_provider: ^2.1.4
  share_plus: ^9.0.0
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
