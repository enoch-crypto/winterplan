# WinterPlan v1.3.2 发版记录

## 版本定位

`v1.3.2` 是月历页面体验优化版本。

## 改动内容

- 月历改成系统日历风格。
- 顶部固定显示 `日 一 二 三 四 五 六`。
- 日期格子里只保留日期数字和事件标记点。
- 去掉每一天格子里的星期文字。
- 日历区域更高、更接近日历样式。
- 底部特殊事件列表和添加按钮保留。

## 验证

- `dart format` 已执行。
- `flutter analyze` 通过。
- `flutter test` 通过。
